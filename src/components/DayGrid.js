import React from 'react'

const DayGrid = ( { showNotesFunc } ) => {
  return (
    <div 
      style={{
        
      }}
      onClick={showNotesFunc}
    >
      DayGrid
    </div>
  )
}

export default DayGrid